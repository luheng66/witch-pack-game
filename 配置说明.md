# 🎮 背包游戏配置指南

## 📖 快速开始

### 方法一：使用可视化配置编辑器（推荐）

1. **打开编辑器**：用浏览器打开 `config-editor.html` 文件
2. **编辑配置**：
   - 像 Excel 一样直接在表格中编辑
   - 点击「➕ 添加物品」或「➕ 添加关卡」增加新行
   - 点击「删除」按钮删除行
3. **导出配置**：点击「💾 导出配置」按钮
4. **替换文件**：编辑器会自动下载 `items.js` 和 `levels.js` 文件，将它们复制到 `assets/data/` 目录覆盖原文件

### 方法二：手动编辑 JS 文件

用任何代码编辑器（VS Code、Notepad++ 等）打开：
- 物品配置：`assets/data/items.js`
- 关卡配置：`assets/data/levels.js`

---

## 📦 物品表配置说明

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| id | string | ✅ | 物品唯一标识 | "potion" |
| name | string | ✅ | 物品名称 | "药水" |
| icon | string | ✅ | 图标文件名（不带后缀） | "potion" |
| width | number | ✅ | 宽度（格子数） | 2 |
| height | number | ✅ | 高度（格子数） | 1 |
| score | number | ✅ | 分值 | 3 |
| type | string | ✅ | 属性类型 | "herb" |
| tag | string | ✅ | 标签描述 | "2x1 草药" |
| color | string | ✅ | 背景颜色（十六进制） | "#9FD39C" |
| description | string | ❌ | 物品描述 | "新鲜采集的草药" |

---

## 🏔️ 关卡表配置说明

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| id | number | ✅ | 关卡ID（从1开始） | 1 |
| day | string | ✅ | 天数标识 | "Day 1" |
| name | string | ✅ | 关卡名称 | "森林采露水" |
| gridCols | number | ❌ | 背包列数（默认6） | 6 |
| gridRows | number | ❌ | 背包行数（默认5） | 5 |
| targetScore | number | ✅ | 通关目标分值 | 18 |
| rules | array | ❌ | 关卡规则 | ["adjacent_bonus"] |
| rulesDescription | string | ✅ | 规则描述 | "相邻加成" |
| availableItems | array | ✅ | 可选物品种类（物品ID） | ["potion", "herb"] |
| brief | string | ✅ | 关卡简介 | "今天要去森林采集晨露。" |
| clearMessage | string | ✅ | 通关提示语 | "背包啪地合上..." |

### 🔄 空值继承规则

如果某个字段留空，会自动继承上一关的值：
- 第1关必须填写所有字段
- 第2关如果 gridCols 不填，会使用第1关的值
- 第3关如果 brief 不填，会使用第2关的值

### 📋 支持的规则

| 规则ID | 说明 |
|--------|------|
| adjacent_bonus | 相邻加成（物品相邻获得加分） |
| forbidden_adjacent | 禁忌相邻（某些物品不能相邻） |
| rotation | 旋转（允许物品旋转） |

---

## 🖼️ 图标资源管理

### 图标文件位置

```
assets/images/icons/
├── potion.svg
├── herb.svg
├── book.svg
└── ...
```

### 支持的格式

- SVG（推荐，矢量图形，缩放不失真）
- PNG
- JPG

### 命名规范

图标文件名 = 物品的 icon 字段 + `.svg`（或其他后缀）

例如：
- 物品 id: "potion", icon: "potion" → 文件名: `potion.svg`
- 物品 id: "magic_book", icon: "book" → 文件名: `book.svg`

---

## 🎯 配置示例

### 添加新物品

```javascript
// 在 itemsTable 中添加
{
  id: "wand",
  name: "魔杖",
  icon: "wand",
  width: 1,
  height: 3,
  score: 6,
  type: "magic",
  tag: "1x3 工具",
  color: "#D4C5E8",
  description: "施放魔法的魔杖"
}
```

### 添加新关卡

```javascript
// 在 levelsTable 中添加
{
  id: 6,
  day: "Day 6",
  name: "魔法森林",
  gridCols: 7,
  gridRows: 6,
  targetScore: 42,
  rules: ["adjacent_bonus", "rotation"],
  rulesDescription: "相邻加成 + 旋转",
  availableItems: ["potion", "herb", "book", "wand"],
  brief: "神秘的魔法森林等待着你！",
  clearMessage: "准备好了！出发去冒险！"
}
```

---

## 🚀 常见问题

### Q: 如何让游戏读取新配置？

A: 配置文件修改后，刷新浏览器页面即可生效。

### Q: 配置了新物品但不显示？

A: 检查以下几点：
1. 物品ID是否唯一
2. 关卡的 availableItems 中是否包含该物品ID
3. 是否有对应的图标文件

### Q: 如何添加更多关卡？

A: 在 levelsTable 数组中添加新对象，id 按顺序递增即可，游戏会自动识别。

### Q: 可以用 Excel 编辑吗？

A: 可以！用 Excel 编辑后另存为 CSV 文件，然后用在线工具转换为 JSON，再手动整理成 JS 格式即可。或者直接使用我们提供的可视化编辑器！

### Q: 如何恢复默认配置？

A: 在可视化编辑器中点击「🔄 重置为默认」按钮即可。
